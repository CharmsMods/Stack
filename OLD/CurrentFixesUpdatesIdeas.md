KEEP IN MIND:

Organization: For any new implementation, fix, update, or change, keep the project organized first. Decide whether the work needs its own subsystem/engine or should extend an existing file cleanly. Prefer the option that keeps the structure maintainable.

Responsiveness: When adding or adapting features, actively balance new functionality with worker-backed CPU/IO execution where practical so long-running work does not lock the ImGui/OpenGL UI. Keep GL resource creation and render passes on the main thread unless a shared-context rendering subsystem is intentionally introduced.

## Current Tracked Work

- [x] Add a reusable worker-backed async system for the native app and move the current CPU/IO-heavy Library and Editor load/export/save flows onto workers where possible, while keeping OpenGL apply/render work on the main thread.
- [x] The Library currently has no button to delete projects, and the delete process should ask the user if they are sure.
- [x] All editor projects should have their latest version of their pipeline, rendered, and saved to a new Assets folder in the library folder that sits next to the program exe, and on library load when the user then clicks on the Assets tab, they should see those images there and be able to fullscreen and download them.
- [x] The user should be able to rename a project in the fullscreen window for it in the library.
- [x] Library Editor projects in fullscreen should have a side panel that shows a parsed, organized, easy-to-read list of all the layers and their settings that were applied to the base image to get to the final edit. There should be a vertical thin slider that moves across the image based on where the mouse is to show before and after for each fullscreen project.
- [x] The current `Save Library` button in the library seems to act more as a `Refresh Library` button instead. The library should automatically refresh and show new projects without having to be manually refreshed, including looking for new projects, updating thumbnails, and updating names.
- [x] Fullscreen library projects when opened should be full quality and not downscaled. Keep the thumbnail downscaled in the library grid, and when the user clicks an editor project, have the native app reach out to the editor engine and render it in full quality for the project window. During processing time, show a spinning loader inside the blank window.
- [x] The medium through which the `.stack` files are structured should be changed, and not base64 and JSON anymore. Move to a binary format that is much faster to parse, and update the loading, saving, and version control systems to handle this. Write a custom parser engine for this that can handle the structure of the editor projects with their layers, settings, and image data, and design the engine to be easy to update to accept other project types from future program tabs for easy future development.
- [x] The `Export Library` button in the library should export the entire library into a binary type file using the new engine from earlier, and include all projects, assets, and settings for each project and any and all data, for the user to load again in a different environment. This button currently does nothing.
- [x] The `Import Json` library button should do the opposite of the export button. It should import those binary files and parse them into the library, and update the library to show the new projects. This button currently does nothing.
- [x] Replace the old `3D Studio` placeholder with Render Phase 1: a dedicated `src/RenderTab/` shell with a panel registry, default dock layout seeding, Window menu, toolbar toggles, detachable/dockable placeholder panels, and local JSON-backed panel state persistence. Keep renderer-core/runtime work out of this phase.
- [x] Start Render Phase 2 after the shell is stable: add the renderer skeleton and scene backbone (`Scene`, `Camera`, `RenderSettings`, `RenderBuffers`, `RenderJob`) plus a deterministic compute-driven viewport path, but still keep BVH/path-tracing/optics features deferred.
- [x] Start the first bounded Render Phase 3 slice on top of the Phase 2 skeleton: add validation scenes, editable spheres/triangles, primitive references, a flat CPU-built BVH, and a compute debug preview that can compare brute-force versus BVH traversal without starting transport features yet.
- [x] Extend the current Render Phase 3 geometry slice with local-space primitives and per-object transforms so bounds generation, BVH rebuilds, compute uploads, and inspector edits all flow through one transform-aware runtime path.
- [x] Extend the current Render Phase 3 geometry slice with reusable built-in mesh definitions and transformed mesh instances that flatten into the shared resolved-triangle/BVH path without starting import or transport work.
- [x] Add Render-local `.renderscene` snapshot save/load so edited validation scenes and custom mesh-backed layouts can round-trip through one scene-plus-camera persistence path before real import/output work begins.
- [x] Add bounded Render viewport fly-camera controls on top of the current Phase 3 geometry/runtime slice so the camera has a real world-space position, the compute preview uses that position directly, and `.renderscene` snapshots persist the free-camera pose without starting viewport gizmos or transport work.
- [x] Convert the interactive Render viewport from a hard-capped preview job into an always-live editor-style surface so the sample goal is soft, the viewport does not freeze at the goal, and Render Manager becomes pause/resume/reset instead of a required restart path.
- [x] Start the first bounded Render Phase 4 kickoff slice on top of the current geometry/BVH foundation: add a visible diffuse multi-bounce `Path Trace Preview` mode with accumulation, bounce limits, and shared BVH/brute-force traversal support, while explicitly deferring emissive/light authoring, MIS, glossy/specular reflection, glass, mist, caustics, denoise, and output work.
- [x] Extend the current Phase 4 kickoff path-trace slice with the first real material/light backbone: scene-owned shared materials, per-primitive material assignment/tinting, emissive-material contribution in the path-trace preview, and a dedicated lit validation scene, while still deferring MIS, glossy/specular reflection, glass, mist, caustics, denoise, and output work.
- [ ] Validate the current Render snapshot-capable fly-camera early-path-trace material/emissive geometry/BVH slice in-app, then continue deeper into baseline path-tracing capabilities without jumping early into MIS, glass, mist, caustics, denoise, EXR/output jobs, or the later optics milestones.
- [ ] Work on adding/adapting the rest of the Editor layers that exist on the web version of the program into the desktop version in the editor tab. Many layers can be handled in a single pass, however a few that should be done separately with more care are specifically the Background Remover layer and all of its functionality, and the Text layer as well.
  Current completed native layer batch from this pass: `Tilt-Shift Blur`, `Dithering`, `Compression`, `Cell Shading`.
  Current completed native layer batch from the next pass: `Heatwave & Ripples`, `Palette Reconstructor`, `Edge Effects`, `Airy Bloom`.
  Current completed native layer batch from the current pass: `Image Breaks`.


Add a "New Project" button to the editor in the Canvas panel