# Architecture Hotspot Map

The highest-value remaining splits are concrete and should be treated as standing cleanup candidates when nearby work happens.

- `src/App/Validation/Suites/DevelopAutoSolveValidationScenarios/`: the Auto-solve validation wall is now split into named scenario fragments. The next refinement is to convert individual fragments into true helper functions with result structs when their data boundaries are clear.
- `src/Editor/Internal/EditorModuleDevelopAutoSolve.cpp`: now owns only the public `EditorModule::ApplyDevelopAutoSolve` coordinator and Auto guidance normalization. Keep it thin: future Develop Auto behavior should land in the focused helper that owns that behavior, not back in this entry point.
- `src/Editor/Internal/EditorModuleDevelopAutoSolveApplication.*`: now owns Auto mode/intent guidance shaping and the selected-solve application orchestration. Keep it as a coordinator that delegates context calculation, RAW authoring, Scene Prep authoring, and Tone/stage writeback to the narrower files below.
- `src/Editor/Internal/EditorModuleDevelopAutoSolveApplicationContext.*`: now owns the shared selected-solve scalar context derived from stats, intent profile, authored guidance, and local-exposure strategy. Keep cross-stage scalar derivation here instead of duplicating formulas in RAW, Scene Prep, or Tone writers.
- `src/Editor/Internal/EditorModuleDevelopAutoSolveRawApplication.cpp`: now owns selected candidate application to authored RAW settings, including exposure placement, white-balance probe application, highlight reconstruction, cleanup, and mosaic denoise settings.
- `src/Editor/Internal/EditorModuleDevelopAutoSolveScenePrepApplication.cpp`: now owns selected candidate application to authored Scene Prep/local-exposure settings, including local range, shadow/highlight, halo, noise, texture, and clamp behavior.
- `src/Editor/Internal/EditorModuleDevelopAutoSolveToneApplication.cpp`: now owns selected-solve requested/achieved integrated Tone JSON writeback, candidate/stage application diagnostics, subject request mirrors, and calibration queueing.
- `src/Editor/Internal/EditorModuleDevelopCandidateGeneration.*`: now owns Guide 03 authored candidate family generation, remembered/rendered rejection lookup, rendered dynamic-range evidence source selection, survivor carry-forward, initial parameter-space merge construction, and the preliminary rendered-feedback handoff. Keep future authored candidate family changes here instead of returning them to the main auto-solve coordinator.
- `src/Editor/Internal/EditorModuleDevelopAutoSolveDiagnostics.*`: now owns broad Develop Auto candidate/stage diagnostic writeback, including `ParameterCandidatesV1`, `CandidateOutcomeLearningV1`, `StagedAutoSolveV1`, `RenderedFeedbackLoopV1`, `RenderedContinuationV1`, convergence/admission aliases, dynamic-range/subject top-level aliases, and rejected-candidate memory writeback. Keep future Auto JSON diagnostic/schema writeback changes here instead of returning them to the main auto-solve coordinator.
- `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackConvergence.*`: now owns rendered-feedback loop constants, continuation-policy records, convergence admission/evidence records, rendered metric readback, history trend checks, same-intent monotonic refine guards, repeated adoption/refinement stop checks, and shared stop-reason convergence classification. Keep future convergence/readback and anti-oscillation fixes here instead of returning them to Auto Solve or Candidate Feedback.
- `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackApplication.*`: now owns authored rendered-feedback application after rendered metrics are available, including direct adoption, rendered-local refinement, selected-vs-best merge, pair merge, ensemble merge, admission bookkeeping, and revision-stage/reason assignment. Keep future rendered-feedback mutation fixes here instead of returning them to the main auto-solve coordinator.
- `src/Editor/Internal/EditorModuleDevelopCandidateGuidance.*`: now owns shared authored guidance adjustment/blending helpers and selected-solve white-balance probe bookkeeping. Keep tiny candidate-guidance math shared here only while it remains broadly useful to candidate generation and rendered-feedback application.
- `src/Editor/Internal/EditorModuleDevelopCandidateScoring.*`: now owns Develop candidate solve/result structs, candidate id/stage taxonomy, continuation-bias scoring helpers, context/final fingerprints, parameter scoring, nearest-survivor distance, and basic scalar damage rejection. Keep future candidate scoring math and candidate-family classification changes here instead of returning them to the main auto-solve coordinator.
- `src/Editor/Internal/EditorModuleDevelopCandidateScoreComponents.*`: now owns `ParameterScoreComponentsV1` and fallback score-component JSON construction for authored parameter candidates. Keep future score-component schema, diagnostic dimension, signal, and risk writeback changes here instead of mixing them into scalar scoring or broad Auto diagnostics.
- `src/Editor/Internal/EditorModuleDevelopDynamicRangeStrategy.*`: now owns Guide 04 dynamic-range stats, compact rendered regional evidence mapping, `DynamicRangeStrategyV1`, `DynamicRangeStrategyMapV1`, `LocalExposureStrategyV1`, and related JSON helpers. Keep future dynamic-range/local-exposure strategy formula changes here instead of returning them to the main auto-solve coordinator.
- `src/Editor/Internal/EditorModuleDevelopSubjectImportance.*`: now owns Guide 05 subject/scene intent resolution, subject-importance map interpretation/refinement, solve-note JSON, subject-importance normalization, and the selected-node viewport/brush bridge. Keep future subject-map solver and viewport handoff fixes here instead of returning them to the main auto-solve coordinator.
- `src/Editor/Layers/ToneCurveLayerModel.cpp`: now owns Tone Curve point editing, curve graph state mutation, curve-input/scene-value coordinate conversion, viewport target/probe state, and final curve evaluation for standalone and Develop-integrated Tone Curve use. Keep future curve model/evaluation fixes here instead of mixing them into shader execution or ImGui surfaces.
- `src/Editor/Layers/ToneCurveLayerAuto.cpp`: now owns Tone Curve auto calibration request bookkeeping, scene readback/analysis, auto intent solving, authored-state construction/preservation, and auto rewrite feedback capture. Keep future auto-analysis/rewrite fixes here instead of mixing them into layer execution or ImGui surfaces.
- `src/Editor/Layers/ToneCurveLayerUI.cpp`: now owns Tone Curve standalone and Develop-integrated ImGui surfaces, resettable Tone sliders, on-image targeting panels, scoped mask panel, graph preview/editor drawing, and expanded node-surface controls. Keep future Tone Curve panel, targeting, graph editor, and surface-control changes here.
- `src/Editor/Layers/ToneCurveLayerSerialization.cpp`: now owns Tone Curve JSON serialization/deserialization, point-array JSON helpers, and persisted auto-authored-state JSON. Keep future Tone Curve schema/default/fallback changes here.
- `src/Editor/Layers/ToneLayerRendering.cpp`: now owns tone-family shader sources, passthrough rendering, GL initialize/execute/destructor methods, and Tone Curve LUT texture upload for Tone Mapper, Tone Curve, Tone Equalizer, View Transform, and Shadows/Highlights. Keep future tone shader and GL execution fixes here.
- `src/Editor/Layers/ToneLayers.cpp`: now owns the smaller non-Curve tone layer UI/serialization bodies plus Tone Curve effective tone/foundation settings and non-rendering control math. Future Tone Curve UI should stay in `ToneCurveLayerUI.cpp`; future effective-setting math can become a separate owner if it grows.
- `src/Renderer/Internal/RenderPipelineGraphExecution.cpp`: now owns the graph traversal contract, recursive image/mask evaluation, cache adoption ordering, node-family routing, and render-target helper call sites inside `RenderPipeline::ExecuteGraphImpl`. Keep fingerprint/cache-key support, scalar socket/Data Math graph analysis, DataMath node execution, framebuffer probes, HDR exposure helper math, HDR Merge graph-source/metadata resolution, generic graph texture cache mutation/pruning, raw source lookup/loading/shared-base rendering, RawDevelop pre-finish/final rendering, Raw Detail graph-node execution/settings resolution, Layer node execution, raw stage cache sizing, raw stage cache clone/find/store/trim/pruning policy, LUT texture upload/cache policy, LUT node dispatch/body, reusable render-target FBO wrappers, and graph context setup out of this file. The next renderer refinement should target another complete node-family branch only when a pass has enough validation coverage around the behavior being moved.
- `src/Renderer/Internal/RenderPipelineGraphExecutionHelpers.*`: now owns graph execution support primitives: FNV/hash helpers, JSON hashing, raw-stage cache sizing limits, quick framebuffer texture probes, default Tone Curve detection, HDR exposure helper math, node/socket key parsing, and `GraphExecutionContext` setup. Keep this file as execution support; do not move render-pass bodies, graph traversal, or graph-side node-family resolvers into it.
- `src/Renderer/Internal/RenderPipelineGraphAnalysis.cpp`: now owns graph-structure analysis used by execution: Data Math average-input collection, first Data Math average-input lookup, channel-socket classification, and recursive scalar-vs-image socket classification. Keep future graph socket-classification and Data Math input-list fixes here instead of returning them to `RenderPipelineGraphExecution.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphDataMathNode.cpp`: now owns graph-side DataMath node execution: scalar/image input resolution, multi-input Average accumulation/division, optional mask/base blending, blank base creation, and simple two-input math dispatch. Keep future DataMath render-execution fixes here; keep input-list and scalar-vs-image graph analysis in `RenderPipelineGraphAnalysis.cpp`, low-level shader pass code in `RenderPipelineNodePasses.cpp`, and generic traversal/cache publication in `RenderPipelineGraphExecution.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphHdrMerge.cpp`: now owns graph-side HDR Merge support: representative-source lookup through graph nodes, first Data Math average input source selection for source tracing, HDR input context resolution from RAW metadata/develop exposure, and resolved HDR exposure/reliability settings. Keep future HDR Merge graph metadata/source-walk fixes here; keep GL rendering/alignment/deghost work in `RenderPipelineHdrMergePass.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphTextureCache.cpp`: now owns generic graph image/mask texture cache lifecycle: cache-entry deletion, full cache destruction, per-key release, store/replace, and inactive-node cache pruning for `m_GraphImageCache` and `m_GraphMaskCache`. Keep future graph cache ownership/lifetime fixes here instead of returning them to `RenderPipelineGraphExecution.cpp` or generic resource lifecycle code.
- `src/Renderer/Internal/RenderPipelineGraphRawStages.cpp`: now owns graph-side RAW source traversal, embedded/file RAW data loading, shared RAW base rendering for RAW Decode and Develop, stage-cache hit adoption, graph-cache publication for raw-base textures, and RAW render failure logging. Keep future RAW source/base-stage fixes here; keep Develop scene prep, integrated tone, hidden pre-finish finalization, and finish-mask behavior in `RenderPipelineGraphRawDevelopNode.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphRawDevelopNode.cpp`: now owns graph-side RawDevelop node rendering after the shared RAW base exists: hidden pre-finish stage-cache lookup, scene-prep rendering, integrated ToneCurve finish execution, finish-mask blending, auto rewrite feedback collection, hidden pre-finish stage-cache publication, and RawDevelop black-output guardrails. Keep future Develop pre-finish/final renderer behavior here; keep RAW source/base rendering in `RenderPipelineGraphRawStages.cpp`, Raw Detail pass primitives in `RenderPipelineRawDetailPasses.cpp`, and stage-cache memory policy in `RenderPipelineRawDevelopStageCache.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphRawDetailNode.cpp`: now owns graph-side Raw Detail Auto Mask / Fusion execution: Raw Detail Auto Mask and Fusion mask-output dispatch, Fusion image-output dispatch, auto-mask-source setting inheritance, debug-preview routing, generated-mask handoff, and Pre-Local Exposure summary publication. Keep future Raw Detail graph dispatch and inherited-setting fixes here; keep low-level Auto Gain, Raw Detail Auto Mask, Raw Detail Fusion, and Pre-Local Exposure shader/pass primitives in `RenderPipelineRawDetailPasses.cpp`.
- `src/Renderer/Internal/RenderPipelineRawDevelopStageCache.cpp`: now owns RawDevelop raw-base/pre-finish stage snapshot cache operations: texture clone, fingerprint lookup with MRU promotion, store/replace, entry deletion, byte accounting, per-key and global budget trimming, cache destruction, and validation-facing cache sizing wrappers. Keep future RawDevelop stage-cache memory policy and lifecycle fixes here, while leaving graph traversal in `RenderPipelineGraphExecution.cpp` and RawDevelop node render behavior in `RenderPipelineGraphRawDevelopNode.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphRenderTargets.cpp`: now owns reusable graph render-target helpers: empty graph target texture creation and the render-into-target FBO wrapper that saves/restores framebuffer and viewport state. Keep future graph render-target boilerplate here instead of duplicating it in node branches.
- `src/Renderer/Internal/RenderPipelineGraphLayerNode.cpp`: now owns graph-side Layer node execution: layer-registry instantiation, JSON deserialization, GL layer execution, ToneCurve auto rewrite feedback collection, default ToneCurve blank-output guardrails, and layer mask blending. Keep future generic Layer node execution fixes here; keep layer shader/runtime implementations in their layer owners and generic graph traversal/cache publication in `RenderPipelineGraphExecution.cpp`.
- `src/Renderer/Internal/RenderPipelineGraphLutNode.cpp`: now owns graph-LUT node dispatch: image/channel input resolution, channel-combine fallback, LUT cache requests, shader uniform binding/draw, optional mask blending, and temporary texture ownership cleanup. Keep future LUT node behavior here; keep texture upload/cache lifecycle in `RenderPipelineLutTextureCache.cpp` and shader/program creation in `RenderPipelinePrograms.cpp`.
- `src/Renderer/Internal/RenderPipelineLutTextureCache.cpp`: now owns graph-LUT texture cache operations: LUT stage hashing, 1D/3D GL texture upload, per-stage texture replacement, cache-key clearing, texture-entry deletion, and inactive-node LUT cache pruning. Keep future LUT GPU upload/cache lifecycle fixes here; keep LUT shader/program setup in `RenderPipelinePrograms.cpp` and LUT node dispatch/uniform binding in `RenderPipelineGraphLutNode.cpp`.
- `src/Editor/Internal/EditorModuleGraphImageNodes.cpp`: now owns image/RAW file import, graph-drop image-chain import scheduling, image payload PNG storage bytes, image-source connection, and image-node rotation. Keep future image/RAW import and runtime image-node mutation fixes here.
- `src/Editor/Internal/EditorModuleGraphMaskNodes.cpp`: now owns scope, mask generator, mask combine, mask utility, Custom Mask, Image-to-Mask node creation, and Tone Curve scoped mask graph construction. Keep future mask-node creation and tone-scope graph mutation fixes here.
- `src/Editor/Internal/EditorModuleGraphProcessingNodes.cpp`: now owns RAW Neural Denoise, Develop, RAW Decode, Raw Detail Auto Mask/Fusion, HDR Merge, and LUT node creation, Develop auto-state trigger/update helpers, Raw Detail hybrid conversion, and manual RAW full-tree construction. Keep future processing-node adders and RAW chain builder fixes here.
- `src/Editor/Internal/EditorModuleGraphMutation.cpp`: no longer owns image/RAW import helpers, mask/tone-scope node creation, or RAW/HDR/LUT processing-node creation. It still owns broad graph/layer mutation, layer/channel split, tone-finish graph helper flows, link/node removal, utility/output adders, and graph metadata refresh. Split the next command family by node/link/group/preset or composite-specific mutation owner when nearby work lands.
- `src/Library/LibraryManager.cpp`: now owns only manager construction/path setup, deferred texture deletion dispatch, and saved-project/UI notification queues. Keep editor project persistence, library scan/refresh/signature logic, thumbnail decode/upload, preview request lifecycle, asset-file lifecycle, project/bundle import-export, Composite-module save/load, conflict resolution, low-level image probing, filename normalization, byte IO, legacy decode, and asset similarity math out of this file.
- `src/Library/LibraryManagerProjects.cpp`: now owns manager-side editor project persistence: project document read/fallback legacy load, editor project save packaging, overwrite, node-browser thumbnail persistence, editor project load/deferred apply handoff, load status transitions, project rename/delete, asset-path derivation, and absolute-path editor project load. Keep future `.stack` editor project save/load, rename/delete, node thumbnail persistence, and project handoff fixes here instead of returning them to the main manager.
- `src/Library/LibraryManagerRefresh.cpp`: now owns library scanning and refresh state: synchronous startup refresh, async refresh snapshots, project/asset metadata scanning, startup trace logging, library signature calculation, auto-refresh throttling, and background signature checks. Keep future scan performance, refresh progress, signature invalidation, and auto-refresh fixes here instead of returning them to the main manager.
- `src/Library/LibraryManagerComposite.cpp`: now owns manager-side Composite-module library persistence: saving a `CompositeModule` project document, loading Composite projects from the library, loading Composite projects from arbitrary disk paths, async status transitions, and Composite current-name/file dirty-state updates. Keep future Composite library save/load fixes here instead of returning them to the main manager or generic import/export file.
- `src/Library/LibraryManagerImportExport.cpp`: now owns manager-side project and bundle import/export workflows: project conflict preview/reset, project conflict resolution, single-project export, library bundle read/write/import/export, bundle finalization, and dropped-file import routing. Keep future `.stacklib`, dropped project/image import, project conflict dialog data, and project export fixes here instead of returning them to the main manager.
- `src/Library/LibraryManagerAssets.cpp`: now owns asset-file lifecycle behavior for the manager: synced project assets, orphan cleanup, loose asset saves, composite embedded asset mirroring, asset import-conflict preview/resolve, asset export, and asset delete. Keep future asset conflict, `.hash` metadata, mirrored embedded asset, and loose image save fixes here instead of returning them to the main manager.
- `src/Library/LibraryManagerThumbnails.cpp`: now owns thumbnail warmup priority, project/asset thumbnail decode scheduling, preview request/cancel flows, texture upload/release, public decode/row-flip compatibility wrappers, and thumbnail PNG generation. Keep future thumbnail cache, preview texture, and async preview lifecycle fixes here instead of returning them to the main manager.
- `src/Library/Internal/LibraryImageHelpers.*`: now owns shared image/preview helpers: binary JSON byte extraction, embedded graph source PNG lookup, meaningful-pixel checks, nearest-neighbor resize, RGBA file load, lightweight image info reads, encoded image/preview decode, row flipping, and project preview pixel resolution. Keep this file narrow; move only reusable image/preview decisions here, not library indexing or UI.
- `src/Library/Internal/LibraryStorageHelpers.*`: now owns reusable Library storage primitives: timestamp formatting, file stem/name normalization, supported extension checks, project/asset filename resolution, byte IO, legacy project decode, asset fingerprint/similarity/conflict math, data-url decode, and image hashing. Keep storage-heavy manager helpers here, but leave workflow ordering, async status, conflict queues, and UI-facing decisions in `LibraryManager.cpp`.
- `src/Library/Internal/LibraryModuleUIHelpers.*`: now owns reusable Library UI primitives: icon texture loading, card sizing/packing, tag/search filter matching, layout hashes, cover UVs, card overlays, compare split handles, project-kind checks, and image fitting. Keep shared visual/layout helpers here so future Library card, preview, and popup splits do not duplicate drawing code.
- `src/Library/LibraryModuleCards.cpp`: now owns project/asset card rendering, per-card hover/selection motion, double-click preview launch state, and card context menus. Keep future grid card interaction and card menu fixes here instead of returning them to the main module.
- `src/Library/LibraryModuleGrid.cpp`: now owns the main Library grid surface: filter-to-card collection, packed layout cache keys, masonry packing application, virtualized visible-card rendering, thumbnail warmup priority, empty-state text, grid context menu placement, tag drawer placement, floating search/options/mode controls, and smooth scroll state. Keep future Library grid layout, search/filter chrome, and scroll-performance fixes here instead of returning them to the main module.
- `src/Library/LibraryModulePreviews.cpp`: now owns project/asset preview modals, animated preview launch/close transitions, preview details panels, preview export/delete/load actions, and preview request cancellation at modal close. Keep future preview modal behavior here instead of returning it to the main module or card renderer.
- `src/Library/LibraryModulePopups.cpp`: now owns global Library popups and popup-adjacent menus: folder import, save-before-load confirmation, project/asset import conflict resolution, Library import/export/refresh menu options, and delete confirmation. Keep future popup flow, conflict dialog, and import/export menu fixes here.
- `src/Library/LibraryModuleTags.cpp`: now owns the Library tag/filter drawer UI: hover-open behavior, drawer animation, tag filter toggles, selected-item tag application, and drawer-local theme preset selection. Keep future tag drawer/filter UI fixes here instead of returning them to the main module.
- `src/Library/LibraryModule.cpp`: still owns the main Library ImGui workflow, icon texture lifecycle, preview open/dismiss state helpers, auto-refresh tick timing, delete-key routing, top-level busy/status overlays, and high-level dispatch into grid/previews/popups. It is now a coordinator; avoid putting full grid, card, preview, popup, tag/filter drawer, or media lifecycle implementations back into it.
- `src/Editor/NodeGraph/EditorNodeGraphUI.cpp`: now owns node layout-cache construction, socket-anchor lookup, render ordering, and node footprint sizing. Canvas render entry/orchestration lives in `src/Editor/NodeGraph/UI/EditorNodeGraphUICanvas.cpp`; interaction handling lives in `src/Editor/NodeGraph/UI/EditorNodeGraphUIInteraction.cpp`; presentation/theme helpers live in `src/Editor/NodeGraph/UI/EditorNodeGraphUIVisuals.*`; link/group drawing lives in `src/Editor/NodeGraph/UI/EditorNodeGraphUILinksAndGroups.cpp`; node body rendering lives in `src/Editor/NodeGraph/UI/EditorNodeGraphUINodes.cpp`. The next graph UI split should target layout-cache construction only if it grows again.
- `src/Editor/Internal/EditorModuleDevelopSubjectControls.*`: now owns Develop Subject Importance region, brush, overlay, and interpreted/refined map display controls. Keep future subject-importance UI changes here.
- `src/Editor/Internal/EditorModuleDevelopAutoGuidanceControls.*`: now owns Develop Auto intent/buttons, guidance sliders, Subject/Scene and Mood/Readability sliders, draft-commit policy, subject-importance control delegation, and Auto reanalysis/interaction reporting for those controls. Keep future Auto guidance UI changes here.
- `src/Editor/Internal/EditorModuleDevelopAutoStatusControls.*`: now owns Develop Auto status/readout text for selected candidates, rendered feedback, timing/readback diagnostics, current solve placement, dynamic-range/local-exposure strategy, subject-map/solve-note summaries, and candidate-feedback quiet-window state. Keep future compact Auto readout changes here.
- `src/Editor/Internal/EditorModuleDevelopManualRawControls.*`: now owns Develop Manual RAW color/exposure controls, RAW exposure draft UI, highlight reconstruction, demosaic status, input-level overrides, orientation, cleanup, mosaic denoise, camera transform, and debug-view controls. Keep future Manual RAW control changes here.
- `src/Editor/Internal/EditorModuleDevelopScenePrepControls.*`: now owns Develop Manual-mode Scene Prep bridge controls, live Pre-Local Exposure summary display, advanced Scene Prep controls, diagnostics controls, and Scene Prep normalization/equality wrappers. Keep future Develop Scene Prep UI changes here.
- `src/Editor/Internal/EditorModuleDevelopFinishToneControls.*`: now owns Develop integrated Finish Tone panel rendering, transient ToneCurve state restore/store, upstream-change notification into auto-prepared tone, and integrated Tone JSON writeback. Keep future integrated Develop tone UI changes here.
- `src/Editor/Internal/EditorModuleDevelopPayloadComparison.*`: now owns exact Develop payload equality checks, including RAW settings, Scene Prep settings, Auto guidance, Subject Importance regions/strokes, integrated tone JSON, and UI mode. Keep future payload-change comparison edits here.
- `src/Editor/Internal/EditorModuleDevelopControls.cpp`: keep the public `RenderRawDevelopControls` entry point stable. It should stay a thin mode/data-flow coordinator; future splits should target only new growth that does not fit the focused helpers above.
- `src/Editor/Internal/EditorModuleDevelopCandidateRequests.cpp`: now owns candidate render request construction, request-budget/admission policy, quiet-window scheduling, subject sampling, option ranking/diversity/reservation, stage scheduler ordering, and request-side validation helpers. Keep future request scheduler work here.
- `src/Editor/Internal/EditorModuleDevelopCandidateRenderPayload.*`: now owns copied candidate render payload mutation for authored guidance probes, including RAW cleanup/detail probe deltas, Scene Prep/local-exposure probe deltas, stage constraints, white-balance probe application, and candidate payload diagnostics. Keep future payload-mapping and stage-freeze math here instead of returning it to request scheduling.
- `src/Editor/Internal/EditorModuleDevelopRenderedCandidateScoring.*`: now owns rendered candidate metric scoring, selected-baseline relative comparison, rendered damage classification, and rendered refine-intent selection. Keep future ranking/classifier threshold fixes here instead of returning them to the feedback writeback file.
- `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackRecords.*`: now owns rendered metrics-to-JSON serialization, bounded rendered-feedback history records, `RenderedFeedbackLoopV1` record writeback, and the JSON hash used to detect feedback persistence changes. Keep future rendered-feedback record/schema plumbing here.
- `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackAnalysis.*`: now owns rendered duplicate detection, pre-finish-distinct preservation decisions, and selected-vs-best final/pre-finish stage-boundary classification. Keep future duplicate thresholds and boundary labels here.
- `src/Editor/Internal/EditorModuleDevelopCandidateFeedback.cpp`: now owns rendered result aggregation, feedback decision flow, rendered rejection-memory orchestration, and integrated Tone JSON application orchestration. Shared loop constants, continuation-policy records, convergence evidence, metric readback, and convergence guards live in `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackConvergence.*`; authored solve mutation from rendered feedback lives in `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackApplication.*`; rendered scoring/classification lives in `src/Editor/Internal/EditorModuleDevelopRenderedCandidateScoring.*`; duplicate/stage-boundary analysis lives in `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackAnalysis.*`; record/history serialization lives in `src/Editor/Internal/EditorModuleDevelopRenderedFeedbackRecords.*`.
- `src/Editor/Internal/EditorModuleDevelopCandidateShared.h/.cpp`: owns the small candidate-stage/shared cache-boundary vocabulary used by both request construction and feedback writeback. Keep it narrow; do not turn it into a generic Develop utility drawer.
- `src/Editor/Internal/EditorModuleRenderRequests.cpp`: now owns preview/composite request admission and request construction. Keep future request breadth, preview deferral, and composite output request work here.
- `src/Editor/Internal/EditorModuleToneCurveViewport.cpp`: now owns tone-curve viewport stats probing, curve-input/final-preview pixel sampling, probe clearing/updating, and target-drag lifecycle for standalone Tone Curve nodes and integrated Develop tone. Keep future tone-curve viewport interaction work here.
- `src/Editor/Internal/EditorModuleRendering.cpp`: now keeps render snapshots, dirty-state orchestration, worker submission/result consumption, tone-curve auto rewrite feedback, and output/cache adoption. The next refinement is to split worker-result consumption if that area grows again.
- `src/Renderer/Internal/RenderPipelineImageGeneratorPass.cpp`: now owns Image Generator texture creation, CPU text canvas generation, UTF-8 text decoding, embedded font access, and stb_truetype rasterization. Keep future generated-image/text rendering work here.
- `src/Renderer/Internal/RenderPipelineHdrMergePass.cpp`: now owns HDR Merge rendering, texture feature readback, reference selection, alignment scoring, deghost setup, and HDR merge shader uniform binding. Keep future HDR merge alignment/render-pass work here.
- `src/Renderer/Internal/RenderPipelineCustomMaskPass.cpp`: now owns Custom Mask CPU raster flattening, object evaluation, blur/morph post-processing, and texture upload. Keep future custom-mask rasterization changes here.
- `src/Renderer/Internal/RenderPipelineNodePasses.cpp`: now owns generated mask, mix, the low-level data math shader pass, utility/image-to-mask, and channel split/combine render-pass helpers. Graph-side DataMath orchestration lives in `RenderPipelineGraphDataMathNode.cpp`; split another remaining pass family only if it grows into a clear owner.
- `src/Renderer/Internal/RenderPipelineRawDetailPasses.cpp`: now owns Auto Gain scene stats, Pre-Local Exposure summary construction, Raw Detail Auto Mask, and Raw Detail Fusion render-pass helpers. Graph-side Raw Detail dispatch and inherited-setting resolution live in `RenderPipelineGraphRawDetailNode.cpp`; keep future scene-prep/local-exposure render math here instead of returning it to `RenderPipeline.cpp`.
